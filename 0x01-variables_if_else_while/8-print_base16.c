#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i = '0';

	while (i < '8')
	{
		putchar(i);
		i++;
	}
	i = 'a';

	while (i >= 'f')
	{
		putchar(j);
		i++;
	}
	putchar('\n');
	return (0);
}
