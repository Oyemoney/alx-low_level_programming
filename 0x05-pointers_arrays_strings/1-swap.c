#include "main.h"

/**
 * swap_int - A function that changes the value of a variable
 * @a: pointer to be replaced
 * @b: pointer to be replaced
 * Return: returns void
 */

void swap_int(int *n)
{
	int n;

	n = *a;
	*a = *b;
	*b = n;
}
