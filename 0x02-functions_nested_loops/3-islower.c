#include "main.h"

/**
 * _islower - A function that prints lowercase ccharactesr
 *
 * Return: returns an integer
 */
int _islower(int c)
{
	char i;

	if (i = 'a' && i <= 'z')
	{
		c = 1;
		return (c);
	}
	else 
	{
		c = 0;
		return (c);
	}
}
